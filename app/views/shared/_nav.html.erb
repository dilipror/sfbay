<div class="bs-example">
    <nav role="navigation" class="navbar navbar-default">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button data-target="#bs-example-navbar-collapse-1" data-toggle="collapse" class="navbar-toggle" type="button">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "Home", root_path, class: 'navbar-brand' %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div id="bs-example-navbar-collapse-1" class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li><%= link_to "Scrape Data", scrape_index_path, method: :post, :confirm => 'Do you want?' %></li>
          </ul>
          <%= form_tag search_scrape_index_path, method: :post, class: "navbar-form navbar-left"  do %>
            <%= text_field_tag :search, @search, class: "form-control", placeholder: "Search by Mileage" %>
            <%= submit_tag "Search", class: "btn btn-default" %>
          <%end%>
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>
  </div>